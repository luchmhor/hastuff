pyscript:
  allow_all_imports: true   # required for requests, pytz, statistics, datetime

input_boolean:
  energy_optimizer_pause:
    name: Pause Energy Optimizer
    icon: mdi:pause-circle

input_button:
  energy_optimizer_retrigger:
    name: Re-run Energy Optimizer
    icon: mdi:refresh
